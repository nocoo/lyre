# osv-scanner suppression list — review quarterly.
#
# All entries below are devDependency-only vulnerabilities that do not reach
# production runtime. Each entry has an ignoreUntil that expires on the next
# review date so osv-scanner will fail loudly if the ignore is not re-evaluated.
#
# Last reviewed: 2026-06-23
# Next review:   2026-09-23
#
# ============================================
# picomatch (indirect devDependencies)
# ============================================
# picomatch 2.3.1 via micromatch, picomatch 4.0.3 via tinyglobby/dotenvx.
# Both are build-/dev-time path matchers that never see attacker-controlled
# glob input in this repo.

[[IgnoredVulns]]
id = "GHSA-3v7f-55p6-f55p"
ignoreUntil = 2026-09-23T00:00:00Z
reason = "Indirect: picomatch via micromatch/tinyglobby (build-time only)"

[[IgnoredVulns]]
id = "GHSA-c2c7-rcm5-vvqj"
ignoreUntil = 2026-09-23T00:00:00Z
reason = "Indirect: picomatch via micromatch/tinyglobby (build-time only)"

# ============================================
# postcss (build-time tailwind/shadcn devDependency)
# ============================================
# GHSA-qx2v-qp2m-jg93 requires parsing untrusted CSS; here postcss only runs
# at build time against developer-authored CSS, so the parsing-error vector
# is not reachable.

[[IgnoredVulns]]
id = "GHSA-qx2v-qp2m-jg93"
ignoreUntil = 2026-09-23T00:00:00Z
reason = "Indirect: postcss via @tailwindcss/postcss/shadcn (build-time only, no untrusted CSS)"
