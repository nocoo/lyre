{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Lyre app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:window:allow-start-dragging",
    "shell:allow-open",
    "dialog:default"
  ]
}
